# Slim requirements for local dev (cloud providers only, no heavy ML)
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9
httpx==0.27.0
aiofiles==23.2.1

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30

# Cache
redis[hiredis]==5.0.4

# AI — LLM
anthropic==0.28.0

# AI — Embeddings (openai only, skip sentence-transformers)
openai==1.30.0

# AI — STT
deepgram-sdk==3.3.0

# AI — Vector DB
qdrant-client==1.9.1

# Config / Utils
pydantic-settings==2.2.1
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.3.0
